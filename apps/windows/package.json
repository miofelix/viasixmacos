{
  "name": "viasix-windows",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "prebuild": "node scripts/fetch-mihomo.mjs && node scripts/fetch-cfst.mjs",
    "prebuild:mihomo": "node scripts/fetch-mihomo.mjs",
    "prebuild:cfst": "node scripts/fetch-cfst.mjs",
    "app:dev": "tauri dev",
    "app:build": "pnpm prebuild && tauri build",
    "app:build:ci": "tauri build",
    "check:version": "node scripts/check-version-align.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0",
    "@tauri-apps/plugin-dialog": "^2.2.1",
    "@tauri-apps/plugin-shell": "^2.2.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
